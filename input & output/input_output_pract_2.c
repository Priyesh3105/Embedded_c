#include<stdio.h>

// scanf(); Reads formatted input from the standard input (usually the keyboard).

int main(){
    int num;
    printf("enter the number: ");
    scanf("%d", &num);
    printf("%d ", num);
    return 0;
}