#include<stdio.h>

int main(){

    int a=1;
    int b=6;
    printf("%d \n", a+++b);
    printf("value of a after above operation is %d\n", a);
    printf("value of b after above operation is %d", b);

    return 0;
}