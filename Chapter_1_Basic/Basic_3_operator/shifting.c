#include<stdio.h>

int main(){
    int var =32;
    printf("value of var after var<<4 is %d\n", var<<4);
    printf("value of var after var>>4 is %d\n", var>>4);
    printf("value of var after ~var is %d\n", ~var);

    return 0;
}